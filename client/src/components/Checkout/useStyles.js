import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  checkoutContainer: {
    paddingTop: 30,
    paddingBottom: 30,
    paddingRight: 10,
    paddingLeft: 10,
  },
});

export default useStyles;
